// ARCHIVED for v1.0 — Newcomer Coach removed from navigation
import { Navigate } from 'react-router-dom';

export default function ClientPortalCoachNewcomerModule() {
  // ARCHIVED: redirect to main coach
  return <Navigate to="/app/client-portal/coach" replace />;
}
